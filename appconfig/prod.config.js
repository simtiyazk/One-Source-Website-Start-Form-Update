/**
 * All the keys, API endpoints, etc go here.
 */

const config = {
  api: 'http:demo.prod/api/v1'
};

export const api = config.api;

export const apiInstagram = `${api}/instagram`;

export const apiTwitter = `${api}/twitter`;

export const apiMedium = `${api}/medium`;

export default config;
